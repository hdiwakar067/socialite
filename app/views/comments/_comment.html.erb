<%= comment.body  %><br>
<strong> <%= comment.commenter  %></strong><br><br>

Commented: <%= time_ago_in_words(comment.created_at) %> ago
<%= link_to "Delete comment", post_comment_path(@post, comment), data: {
  turbo_method: :delete,
  turbo_confirm: "Are you sure?"
} %><hr>
